{
  "tools": [
    {
      "name": "list_cloudwatch_dashboards",
      "description": "List all CloudWatch dashboards in the AWS account",
      "inputSchema": {
        "type": "object",
        "properties": {
          "accountId": {
            "type": "string",
            "description": "AWS account ID for cross-account access"
          },
          "roleName": {
            "type": "string", 
            "description": "IAM role name for cross-account access"
          }
        }
      }
    },
    {
      "name": "fetch_cloudwatch_logs_for_service",
      "description": "Fetch recent CloudWatch logs for a specific AWS service",
      "inputSchema": {
        "type": "object",
        "properties": {
          "serviceName": {
            "type": "string",
            "description": "Name of the AWS service (e.g., lambda, ec2, rds, eks, etc.)"
          },
          "accountId": {
            "type": "string",
            "description": "AWS account ID for cross-account access"
          },
          "roleName": {
            "type": "string",
            "description": "IAM role name for cross-account access"
          }
        },
        "required": ["serviceName"]
      }
    },
    {
      "name": "get_cloudwatch_alarms_for_service", 
      "description": "Get CloudWatch alarms for a specific service",
      "inputSchema": {
        "type": "object",
        "properties": {
          "serviceName": {
            "type": "string",
            "description": "Name of the AWS service to get alarms for"
          },
          "accountId": {
            "type": "string",
            "description": "AWS account ID for cross-account access"
          },
          "roleName": {
            "type": "string",
            "description": "IAM role name for cross-account access"
          }
        },
        "required": ["serviceName"]
      }
    },
    {
      "name": "setup_cross_account_access",
      "description": "Setup and verify cross-account access for monitoring",
      "inputSchema": {
        "type": "object",
        "properties": {
          "accountId": {
            "type": "string",
            "description": "AWS account ID to access"
          },
          "roleName": {
            "type": "string",
            "description": "IAM role name to assume in the target account"
          }
        },
        "required": ["accountId", "roleName"]
      }
    },
    {
      "name": "list_log_groups",
      "description": "List all CloudWatch log groups in the account",
      "inputSchema": {
        "type": "object",
        "properties": {
          "accountId": {
            "type": "string",
            "description": "AWS account ID for cross-account access"
          },
          "roleName": {
            "type": "string",
            "description": "IAM role name for cross-account access"
          }
        }
      }
    },
    {
      "name": "analyze_log_group",
      "description": "Analyze a specific CloudWatch log group for patterns and errors",
      "inputSchema": {
        "type": "object",
        "properties": {
          "logGroupName": {
            "type": "string",
            "description": "Name of the CloudWatch log group to analyze"
          },
          "accountId": {
            "type": "string",
            "description": "AWS account ID for cross-account access"
          },
          "roleName": {
            "type": "string",
            "description": "IAM role name for cross-account access"
          }
        },
        "required": ["logGroupName"]
      }
    },
    {
      "name": "get_dashboard_summary",
      "description": "Get summary and configuration of a specific CloudWatch dashboard",
      "inputSchema": {
        "type": "object",
        "properties": {
          "dashboardName": {
            "type": "string",
            "description": "Name of the CloudWatch dashboard"
          },
          "accountId": {
            "type": "string",
            "description": "AWS account ID for cross-account access"
          },
          "roleName": {
            "type": "string",
            "description": "IAM role name for cross-account access"
          }
        },
        "required": ["dashboardName"]
      }
    }, 
    {
    "name": "create_incident_jira_ticket",
    "description": "Create a simple issue in the Jira project backlog. Project and issue type are configured via environment variables.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "summary": {
          "type": "string",
          "description": "Short title for the issue (under 120 characters)."
        },
        "description": {
          "type": "string",
          "description": "Detailed description of the issue or incident."
        }
      },
      "required": ["summary", "description"]
      }
    }
  ]
}